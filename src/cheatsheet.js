
/*
// Tailwind CSS Cheat Sheet

// Layout
.container { @apply container; }
.flex { @apply flex; }
.grid { @apply grid; }

// Flexbox
.items-center { @apply items-center; }
.justify-center { @apply justify-center; }

// Spacing
.p-4 { @apply p-4; }
.m-4 { @apply m-4; }

// Sizing
.w-full { @apply w-full; }
.h-full { @apply h-full; }

// Typography
.text-lg { @apply text-lg; }
.font-bold { @apply font-bold; }

// Backgrounds
.bg-red-500 { @apply bg-red-500; }

// Borders
.border { @apply border; }
.rounded { @apply rounded; }

// Effects
.shadow { @apply shadow; }

// This is just a basic cheat sheet. Tailwind CSS has a lot more utility classes.
// For a full list, check out the official documentation: https://tailwindcss.com/docs
*/
